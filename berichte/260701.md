# Arbeitsbericht vom 17.06.2026

- Thema: Text und Binär
- Name: Lemuel Butuman
- Klasse: 2AHITS
- Fach: ITS1

## Algemeine Daten

1. Daten werden in Binär dargestellt damit diese für Menschen lesbar sind wurd `Ascii` erfunden dort sind die Zeichen in einem Byte gespeichert
2. Es gibt viele verschiedene Sonderzeichen wie `ä` dafür gab es keinen Platz deswegen wurden Pages entwickelt. 
3. Dadurch ist aber ein großer Chaos enstanden deswegen wurde der `Unicode` eingeführt, dieser fasst alle Zeichen in 4 Byte zusammen, da dies aber sehr viel Speicher fressen, wurde für dieses problem ein system ausgedacht.

das sagt: 

Wenn der vorderste byte nicht gesetzt wurde ist es ein Zeichen aus der Ascii Tabelle sonst wird durch schlaue Byte setzung geschaut was für ein Zeichen es ist dies wurde UTF-8 genannt.

## 1. xxd

Der Befehl `xxd` sorgt dafür das eingegebene Normale Buchstaben in Asc Code angezeigt werden.

bsp:
```sh
echo "a" | xxd
00000000: 610a                                     a.
```

### Optionen `-n -r -p` 

`-n` sorgt dafür das echo keinen Zeilenumbruch mehr dranhängt.

`-p` sorgt dafür das xxd nur mehr der Ascii Code angezeigt wird und nicht mehr die Stelle wo es liegt in Hex und was es in Normalen Zeichen ist.

`-r` sorgt dafür das xxd rückwärts funktionert also aus Ascci normale Zeichen macht.


bsp mit Emojis:

```sh
echo -n "😃" | xxd -p
f09f9883
```
```sh
echo -n "f09f9883" | xxd -r -p
😃
```

## Aufgabe

Aufgabe: Übung (UTF-8) Ermittle durch shell Befehle die UTF-8 Kodierung der Zeichen: 🥳😎👽...

```sh
~/workspace$ echo -n "🥳" | xxd -p
f09fa5b3
~/workspace$ echo -n "😎" | xxd -p
f09f988e
~/workspace$ echo -n "👽" | xxd -p
f09f91bd
~/workspace$ echo -n "©" | xxd -p
c2a9
~/workspace$ echo -n "€" | xxd -p
e282ac
~/workspace$ echo -n "ß" | xxd -p
c39f
```

## Hex Dump

Es ist mit der Option `-b` möglich diese steht für binary dump.

bsp

```sh
echo -n "A" | xxd -b
echo -n "A" | hexdump -b
```

Manchmal wird der in dem Zusammenhang (vor allem in der Netzwerktechnik) der Begriff “octet” verwendet. Was ist die Bedeutung? Gibt es einen Grund diesen Begriff zu verwenden?

Ein `octect` sind genau 8 Bit dies wurde früher verwedent da ein Byte nicht immer 8 Bit groß war, dies wurde dann z.B. bei Ips verwendet das alles einheitlich ist.


`xxd` unterstützt eine Option `-E`. Was macht diese? Ist diese heutzutage noch relevant?

Die Option -E sorgt dafür das die EBCDIC-Zeichentabelle anstelle von Ascii verwedent wurde, diese Option ist heutzutage so ziemlich unnötig nur bei alten Sytemen kann sie manchmal noch Verendung finden.


Um einen dezimalen Wert (ASCII Ziffern) als Binärwert in ein File zu schreiben können folgende Befehle verwendet werden

$ value=65534
```  
$ printf "%.8x" $value | xxd -r -p >data.bin
```

Was macht das printf und der Ausdruck %.8x?

Wie würdest du jemanden (mit einem Bash Grundverständnis) erklären wie diese Zeile die Aufgabenstellung löst.


Finde einen Weg um den 4 Byte Binärwert aus der Datei wieder als Dezimalwert darzustellen. 

$ printf "%.8x" $value gibt den Wert Value in Hexadezimal aus da %.8x sagt das mindestens 8 Stellen in hexadezimal ausgegeben werden müssen.


Die Zeile wandelt den wert im Value zuerst in Hexadezimal um, danach wird diese wieder in Binär umgewandelt und in die Datei geschrieben

```sh
echo $((16#$(xxd -p data.bin)))

65534
```


Was macht die -i Option von xxd? Was könnte eine mögliche Anwendung sein?

`xxd -i` gibt den Inhalt direkt als C Datei aus, es ist dafür da eine Binärdatei direkt in ein C Programm einzubauen


## Windows Editor

Erstelle mit dem Windows Editor eine Textdatei (soll auch die Zeichen äüöÄÜÖß€ enthalten) speichere einmal in UTF-8 und einmal in ANSI Kodierung
Zeige mit cat den Inhalt beider Files an. Welcher Unterschied ist feststellbar? Was wird die Standard Kodierung der REPL Kommandozeile sein?

```sh
cat Ansi.txt
O
wasd

cat Utf8.txt 
äüöÄÜß€
wasd
```


Analysiere den Inhalt beider Files per Hexdump: wie werden in beiden Varianten die Buchstaben Ö, ß und € kodiert

Ansi: Ö = d6, € = 80, ß = df
UTF-8: Ö = 96c3, € = 82e2ac, ß = 9fc3


Wie werden Zeilenumbrüchen in den unter Windows erstellten Dateien dargestellt? Wie sieht das in einer Datei aus die unter Linux (z.B. REPL) erstellt wurde?

```sh
Linux: 0a
Windows: 0a0d

```


Konvertiere in der Kommandozeile (mit iconv) ANSI (auch code page 1252 genannt, kurz: CP-1252) nach utf-8 und prüfe das Ergebnis.

```sh
~/workspace$ iconv -f CP1252 -t utf-8 Ansi.txt -o AnsitoUtf.txt

~/workspace$ hexdump Ansi_to_Utf-8.txt 
0000000 a4c3 bcc3 b6c3 84c3 9cc3 96c3 9fc3 82e2
0000010 0dac 770a 7361 0064                    
0000017
```


Forschung/Recherche: Was ist der Unterschied zwischen ANSI und Latin-1?


## Weiter bin ich leider nicht gekommen und habs leider verspätet abgegeben 